{
  "name": "sendformdata",
  "version": "0.3.0",
  "description": "send form data function for netlify",
  "main": "sendformdata.js",
  "scripts": {
    "start:lambda": "netlify-lambda serve src/functions",
    "build:lambda": "netlify-lambda build src/functions && node -p 'process.env'"
  },
  "keywords": [
    "serverless function",
    "node netlify-lambda",
    "send form data"
  ],
  "author": "Marc",
  "license": "ISC",
  "dependencies": {
    "@sendgrid/mail": "^6.4.0",
    "axios": "^0.21.1",
    "netlify-lambda": "^1.6.3"
  },
  "devDependencies": {
    "dotenv": "^8.1.0"
  }
}
